namespace Elenor {
    /// <summary>
    /// Identifies who or what dealt damage.
    /// </summary>
    public enum DamageSource {
        Unspecified,
        PlayerProjectile,
        PlayerDash,
        Poison,
        Enemy,
    }
}